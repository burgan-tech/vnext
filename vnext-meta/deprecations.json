{
  "items": [
    {
      "id": "direct-trigger-task-version",
      "type": "field",
      "component": "task",
      "path": "directTriggerTask.version",
      "deprecatedSince": "0.0.26",
      "removedAt": null,
      "replacement": null,
      "severity": "warning",
      "message": "Version is not used for direct trigger tasks"
    },
    {
      "id": "workflow-execution-transition-per-job",
      "type": "config",
      "component": "runtime",
      "path": "WorkflowExecution.TransitionPerJob",
      "deprecatedSince": "0.0.80",
      "removedAt": "0.0.80",
      "replacement": "WorkflowExecution.AutoTransitionMode",
      "severity": "error",
      "message": "Removed. Its boolean shape could not express the new inline default, and it was gated on an outbox flag that no longer exists. Use AutoTransitionMode: Scheduled for the old true behavior; Inline (the default) is the new one."
    },
    {
      "id": "workflow-execution-direct-enqueue-continuations",
      "type": "config",
      "component": "runtime",
      "path": "WorkflowExecution.DirectEnqueueContinuations",
      "deprecatedSince": "0.0.80",
      "removedAt": "0.0.80",
      "replacement": null,
      "severity": "error",
      "message": "Removed with the transactional-outbox continuation path. The scheduler is now the only delivery path; a failed enqueue is retried briefly and then reported instead of being deferred to the Inbox relay."
    },
    {
      "id": "workflow-execution-latest-only-instance-loading",
      "type": "config",
      "component": "runtime",
      "path": "WorkflowExecution.LatestOnlyInstanceLoading",
      "deprecatedSince": "0.0.80",
      "removedAt": "0.0.80",
      "replacement": null,
      "severity": "error",
      "message": "Removed. Latest-only instance loading is now unconditional — the canary rollout it gated has completed. History readers must use the repository full-history APIs."
    },
{
      "id": "metric-script-executions-total-compile-semantics",
      "type": "behavior",
      "component": "runtime",
      "path": "prometheus.script_executions_total",
      "deprecatedSince": "0.0.85",
      "removedAt": null,
      "replacement": "script_compilations_total (compile-or-fetch calls, result=hit|miss) and script_execution_duration_seconds_count (true script executions by script_type)",
      "severity": "warning",
      "message": "script_executions_total has always been incremented on the COMPILE path (cache hits included), not on script execution. It keeps emitting unchanged for backward compatibility; migrate dashboards/alerts to script_compilations_total and script_execution_duration_seconds, then this metric will be removed in a future release."
    },
    {
      "id": "extension-scope-get-history-transition-removed",
      "type": "field",
      "component": "extension",
      "path": "extension.scope",
      "deprecatedSince": "0.0.85",
      "removedAt": "0.0.85",
      "replacement": null,
      "severity": "warning",
      "message": "The ExtensionScope member GetHistoryTransition was removed. It shared its numeric value (2) with GetAllInstances, was never accepted by the extension component schema (scope allows 1, 2, 3), was never handled by the scope switch and was never referenced by any code path, so no authored extension could request it and no stored value ever meant it. Extensions on the transitions-history endpoint are not supported; reinstating that capability requires a new scope value plus runtime wiring."
    }
  ]
}
