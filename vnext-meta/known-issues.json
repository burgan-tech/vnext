{
  "items": [
    {
      "id": "automatic-transition-chain-depth-capped",
      "affectedVersions": ">=0.0.26",
      "severity": "error",
      "component": "transition",
      "path": "states[].transitions[] (automatic chains)",
      "title": "Automatic transition sync dispatch stops when chain depth exceeds 50; the pipeline fails with error code Transition:100017.",
      "workaround": "Refactor the workflow so automatic chains do not exceed the depth guard, insert intermediate human states, or split logic across transitions.",
      "fixedIn": null
    },
    {
      "id": "scheduled-transition-without-timer-skipped",
      "affectedVersions": ">=0.0.26",
      "severity": "warning",
      "component": "timer",
      "path": "states[].scheduledTransitions[].timer",
      "title": "If a scheduled transition has no timer configuration, ScheduleTransitionsStep logs a warning and skips enqueueing that schedule (transition does not fire from scheduling).",
      "workaround": "Ensure every scheduled transition defines a timer evaluated by the timer service.",
      "fixedIn": null
    },
    {
      "id": "workflow-timeout-mapping-fallback-static-duration",
      "affectedVersions": ">=0.0.26",
      "severity": "warning",
      "component": "timer",
      "path": "workflow.timeout.mapping",
      "title": "When dynamic workflow timeout mapping fails, the runtime falls back to the static ISO 8601 duration from workflow timeout configuration.",
      "workaround": "Fix or simplify the timeout mapping script; verify TimeoutMappingFallback logs during instance start.",
      "fixedIn": null
    },
    {
      "id": "workflow-timeout-job-scheduling-failure-non-blocking",
      "affectedVersions": ">=0.0.26",
      "severity": "warning",
      "component": "instance",
      "path": "workflow.timeout",
      "title": "If enqueueing or persisting the workflow timeout background job throws, the failure is logged but instance start continues without blocking.",
      "workaround": "Monitor WorkflowTimeoutSchedulingFailed logs and job infrastructure; retry or reconcile timeouts externally if needed.",
      "fixedIn": null
    },
    {
      "id": "cancel-scheduled-jobs-failure-does-not-block-transition",
      "affectedVersions": ">=0.0.26",
      "severity": "warning",
      "component": "timer",
      "path": "lifecycle.CancelScheduledJobsStep",
      "title": "When cancelling scheduled transition jobs for the leaving state fails, the pipeline logs ScheduledJobsCancellationFailed and continues the transition anyway.",
      "workaround": "Inspect cancellation/job-store failures; manually reconcile orphaned scheduled jobs if transitions completed despite cancellation errors.",
      "fixedIn": null
    },
    {
      "id": "async-transition-enqueue-lock-race-silent-loss",
      "affectedVersions": ">=0.0.26 <0.0.69",
      "severity": "error",
      "component": "transition",
      "path": null,
      "title": "Async (sync=false) transition jobs could fire while the enqueue-side distributed lock still held the same instance key; the job consumer failed with a lock conflict that was silently swallowed, losing the transition and stranding the instance in Busy status.",
      "workaround": "Retry the transition after the instance is recovered; upgrade to a fixed version where the enqueue lock is scoped with an :enqueue suffix and the job handler retries lock conflicts and faults the instance on exhaustion.",
      "fixedIn": "0.0.69"
    },
    {
      "id": "duplicate-async-transition-job-conflict",
      "affectedVersions": ">=0.0.26",
      "severity": "info",
      "component": "transition",
      "path": null,
      "title": "A second async transition request for the same instance and transition key while a job is already active is rejected with HTTP 409 Conflict.",
      "workaround": "Use idempotency keys, poll instance state, or wait for the in-flight job to finish before retrying.",
      "fixedIn": null
    },
    {
      "id": "instance-data-numeric-precision-loss",
      "affectedVersions": "<=0.0.85",
      "severity": "warning",
      "component": "instance",
      "path": "instanceData.attributes (numbers)",
      "title": "Instance data appends reformat every number through int/double, so integers above 2^53 (including values that no longer fit int64) and decimals beyond ~15 significant digits are persisted with reduced precision — and the rewrite touches numbers the append never referenced.",
      "workaround": "Set WorkflowExecution:InstanceDataWrite:PreserveNumericPrecision=true to canonicalize numbers losslessly (int64 exact, decimal in plain notation; values beyond decimal's 28-29 significant digits are still rounded). Enabling it changes the content hash once per affected instance — for values that lost precision, for values currently written in exponent notation (0.00001 becomes 0.00001 instead of 1E-05) and for a negative zero with a fraction (-0.0 becomes 0) — costing one extra version row and one phantom diff there, with no data loss. Alternatively carry exact monetary values as strings in the schema.",
      "fixedIn": null
    },
    {
      "id": "enqueue-relay-drops-subflow-chain-reserve-claim",
      "affectedVersions": ">=0.0.80 <=0.0.92",
      "severity": "error",
      "component": "execution",
      "path": "POST .../transitions/{key}/enqueue (TransitionContinuationRequested relay)",
      "title": "The internal enqueue relay's event-to-payload copy omitted SubflowChainReserved, so with WorkflowExecution:DirectEnqueueContinuations=false (or when the direct Dapr enqueue falls back to the outbox) an async transition on a parent with an active SubFlow reached the leaf without the chain-reserve claim its own accept had taken, and the leaf rejected the forward as Busy (Instance:100031).",
      "workaround": "Keep DirectEnqueueContinuations=true (the default); the drop then only bites when the direct Dapr enqueue fails and the outbox fallback carries a subflow-forward transition. Retrying the transition after the fallback window is the only recovery on affected versions.",
      "fixedIn": null
    },
    {
      "id": "arming-poller-not-hosted-orphans-arm-failures",
      "affectedVersions": "<=0.0.92",
      "severity": "error",
      "component": "background-jobs",
      "path": "BackgroundJob:WithHostedService (orchestration appsettings + vnext-runtime templates)",
      "title": "The Aether arming poller is registered only by the Orchestration host and switched off there (WithHostedService=false, also in the vnext-runtime deployment templates), so no process arms recovery. When the immediate Dapr scheduler arm fails (scheduler restart/outage at accept, timer arm, ack-timeout arm), Aether rolls the job row back to Pending 'for the arming poller' - and nothing ever arms it. The async accept has already answered 202 and reserved the instance, so the instance stays Busy forever; retries are rejected by the Busy check and the duplicate-active-job guard. Verified live (issue #935 chaos test): a Pending row sat unarmed for 15 minutes with the scheduler healthy again; enabling the poller recovered it within seconds.",
      "workaround": "Set BackgroundJob:WithHostedService=true on exactly one host (orchestration) so the arming poller and reapers run. A stuck instance can be recovered manually by setting its sys_queues.BackgroundJobs row Status back to 5 (Pending) with the poller enabled.",
      "fixedIn": null
    },
    {
      "id": "past-due-oneshot-arm-race-orphans-scheduled-row",
      "affectedVersions": "<=0.0.92",
      "severity": "error",
      "component": "background-jobs",
      "path": "Aether JobDispatcher / BackgroundJobArmingProcessor (upstream: aether)",
      "title": "Arming a past-due one-shot races the Dapr scheduler's immediate fire: the job callback can arrive before the armer commits Pending->Scheduled, JobDispatcher answers 'not found in an active state; skipping' with HTTP 200, Dapr deletes the consumed one-shot, and the armer then commits Scheduled - leaving a Scheduled row with no scheduler entry, permanently. The VisibilityTimeout reaper covers Running rows only, so nothing recovers it. Observed live (issue #935 chaos test, outbox mode + poller on): dispatcher skip at 12:17:38Z, row Scheduled for 11+ minutes, instance Busy. Aether-level defect; the same orphan also results from a failed rollback after a direct-arm timeout whose server-side registration succeeded.",
      "workaround": "Manual: set the orphaned sys_queues.BackgroundJobs row Status back to 5 (Pending) so the poller re-arms it (Dapr one-shot arming is overwrite-idempotent). No configuration prevents the race.",
      "fixedIn": null
    },
    {
      "id": "discovery-domain-list-single-page-ceiling",
      "affectedVersions": ">=0.0.92",
      "severity": "warning",
      "component": "runtime",
      "path": "ServiceDiscovery:Cache (bulk warm-up)",
      "title": "The registry's domain-list function serves a single page (500 rows today) and its response carries no truncation signal, so a registry holding more domains than that warms only the first 500.",
      "workaround": "The runtime logs WorkflowLogs 50038 DomainListCeilingReached at Warning when the list comes back at or above ServiceDiscovery:Cache:DomainListExpectedMax, and publishes it anyway - the domains beyond the ceiling are simply not warmed and each pays a live per-domain lookup, which is the behaviour with the cache switched off. Raising the ceiling is a change on the registry side (vnext-domain-discovery, DomainListMapping.csx pageSize).",
      "fixedIn": null
    },
    {
      "id": "workflow-timeout-reset-not-implemented",
      "affectedVersions": ">=0.0.1",
      "severity": "warning",
      "component": "runtime",
      "path": "workflow.timeout.timer.reset",
      "title": "timer.reset is required by the schema but is read NOWHERE in the runtime, so a workflow timeout always means 'not finished within duration, counted from instance start' and never 'idle for duration'. Whatever the field says - 'never', 'OnEntry' - the behaviour is identical.",
      "workaround": "Author the duration as an absolute budget from start and do not rely on activity resetting it; every authored value in the reference corpus today declares OnEntry against a runtime that ignores it. The instant is resolved once when the scheduler is armed (mapping script included) and never re-armed, which is also why the state function's timeout.executeAtUtc is stable for the instance's whole life. Expressing an idle timeout needs a per-state scheduled transition (triggerType 2), which IS cancelled and re-armed on every state entry.",
      "fixedIn": null
    },
    {
      "id": "state-level-subprocess-rejected-at-publish",
      "affectedVersions": ">=0.0.93",
      "severity": "error",
      "component": "workflow",
      "path": "states[].subFlow.type",
      "title": "A state starts a SubFlow and only a SubFlow: state.subFlow.type must be 'S'. A SubProcess is started by its own task (SubProcessTask, TaskType.SubProcess = 14), which creates the correlation itself, not by a state. WorkflowValidator now rejects a state-level subFlow.type of 'P' at publish. This is not a regression: the shape never worked on the sync path - it returned 409 Instance:100031 and left the parent Busy and not Faulted with no automatic recovery - and only appeared to work when driven asynchronously, where a job re-entry happened to carry IsPreReserved.",
      "workaround": "Model the SubProcess as a SubProcessTask on a transition or state hook instead of as state.subFlow, keeping the child workflow itself unchanged.",
      "fixedIn": null
    },
    {
      "id": "pre-reserved-job-failure-can-strand-busy",
      "affectedVersions": ">=0.0.26",
      "severity": "error",
      "component": "transition",
      "path": null,
      "title": "An async accept flips Busy before answering 202, and a job re-entry that fails with a non-lock-conflict Result error (for example a policy re-validation failure) logs JobFailed and returns without faulting, so that Busy can be left with no owner - the pipeline's own fault path covers only failures inside the step loop, retry requires Faulted, and the sync path leaves no job row for recovery. Note that a blanket \"fault on any job failure\" is not the fix: it faults a parent that is legitimately Busy for its subflow child's lifetime, which was measured to break a working chain (ChainBusy.PostCommitForwardFailure_ReleasesTheChainReserve_SoNoLevelStaysBusy, root left Faulted in root-waiting with a JOB_EXECUTION_FAILED incident). A correct fix needs a predicate for \"this execution owns that Busy\", which the job handler does not have today.",
      "workaround": "Recover a stranded instance manually with cancel/exit; prefer sync for transitions whose policy may change between accept and re-entry.",
      "fixedIn": null
    }
  ]
}
