{
  "runtimeVersion": "0.0.92",
  "engine": {
    "dbMigratorDowngrade": {
      "since": "0.0.92",
      "status": "stable",
      "description": "DbMigrator gains explicit commands beside its unchanged no-argument forward migration. 'status' reports, per schema, the applied migration head, the migrations pending against the running build, and any applied migrations the build does not know. 'downgrade --target <migrationIdOrName>' CONVERGES every workflow schema (system schemas plus every flow schema discovered from sys_flows, or an explicit --schema subset) to the named migration: schemas above it revert (Down() methods run newest-first, one transaction per migration, per-schema history maintained), schemas below it catch up - self-healing for mixed-level fleets. '--messaging-target' does the same for the independent sys_queues messaging chain. Safety gates: the whole run refuses when any schema's history holds migrations the running build does not ship (a Down() exists only in the build that shipped it - rollback must run from the NEWER image before deploying the older one); reverts that drop, rename or re-type a table/column THIS build's compiled EF model maps are refused without --for-runtime-rollback (the operator's declaration that an OLDER runtime deploys right after; without it a same-version runtime would fail 42703/42P01 at first touch of the affected entity - index/constraint/trigger-only reverts are model-neutral and pass); destructive reverts (DropTable/DropColumn/DropSchema) additionally require --accept-data-loss; target '0' (full wipe) is rejected; Runtime:EnableSchemaMigration=false refuses the command. '--script <dir>' is a dry run writing reviewable per-schema SQL (schema-qualified, history bookkeeping included) without applying; the model-break and data-loss findings surface as warnings there instead of blocking. Every argument has a DbMigrator:* configuration/environment fallback (DbMigrator__Command=downgrade etc.) because deployment templates pass no container args. Applies run in parallel per schema under the same schema-migration:{schema} distributed lock as the forward path; exit code contract unchanged (any failed schema => exit 1).",
      "apiEndpoints": [],
      "relatedConfig": [
        "DbMigrator:Command",
        "DbMigrator:Target",
        "DbMigrator:MessagingTarget",
        "DbMigrator:Schemas",
        "DbMigrator:ScriptDirectory",
        "DbMigrator:AcceptDataLoss",
        "DbMigrator:ForRuntimeRollback",
        "Runtime:EnableSchemaMigration",
        "SchemaMigration:CommandTimeoutSeconds",
        "SchemaMigration:LockExpirySeconds"
      ]
    },
    "instanceStartType": {
      "since": "0.0.92",
      "status": "stable",
      "description": "metadata.type on the instance projection - how the instance was STARTED: R root, S SubFlow child, P SubProcess child. Stamped once at creation and never updated, so the origin stays answerable long after the instance has finished, which is what makes it usable for reporting. Derivation, applied once inside the aggregate in Instance.SetInfoMetadata: a non-empty parent.id in the start metadata means the instance was started as a child and parent.flowtype says which kind; anything else is R. parent.flowtype ALONE is deliberately not consulted, because SetInfoMetadata TryAdds the instance's OWN workflow type code into that key when it is absent - so a workflow whose definition declares type \"S\" and is started directly through the API carries parent.flowtype \"S\" with no parent at all, and is correctly reported as R. PURELY ADDITIVE: the parent.* ExtraProperties contracts are untouched and remain the source of truth for the parent/child relationship itself, and Instance.IsSubFlow / IsSubItem keep their existing behaviour including that mislabel; type records the start origin, not a live relationship. Served as metadata.type on GET .../instances/{instance}, on every item of the list view and on the GetInstanceTask (type 19) projection - one constructor feeds all three. NOT carried on sync start/transition responses and NOT in the state function body, so StateFunctionCache.ResponseShapeVersion stays v9 and the state fingerprint is unchanged (an immutable column can never invalidate a cached response). The rule script context (instance.Type) and the role-grant context ($.context.Instance.Type) carry the bare code. Filterable and sortable as instanceType - accepting names or codes (Root/R, SubFlow/S, SubProcess/P) with eq/ne/in/nin only - and deliberately NOT under the bare name type: instance columns and data attributes share one namespace on the legacy and GraphQL filter surfaces and are told apart purely by name, so claiming type would silently retarget the existing filters of every domain whose schema has a business field called type. The .First()/.Last() selector terminal has no name resolution for any short-code column and takes codes only. Existing rows are backfilled by the AddInstanceType migration from the parent block.",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}",
        "GET {domain}/workflows/{workflow}/instances"
      ]
    },
    "instanceEffectiveStatus": {
      "since": "0.0.92",
      "status": "stable",
      "description": "metadata.effectiveStatus on the instance projection - the status counterpart of metadata.effectiveState, and the value a client should branch on. Instance.EffectiveStatus has existed as a column since the EffectiveStatus migration but was state-function ETag fingerprint material only and was never served; it is now exposed as metadata.effectiveStatus on GET .../instances/{instance}, on every item of the list view, on the GetInstanceTask (type 19) projection, and as effectiveStatus on sync start/transition responses (an async 202 does not carry it - it answers from the admission decision with no instance projection in hand). Semantics: the deepest active SubFlow's status while one is running, otherwise the instance's own status. metadata.status is unchanged and still reports the instance's own row status, which is Busy for a parent's entire child lifetime by design and therefore cannot distinguish a child waiting on a human task from real work. Responses read Instance.GetEffectiveStatus, not the raw column: the propagated projection is served only while NEITHER side is terminal, otherwise the instance's own status answers. Both arms match the state function, so metadata.effectiveStatus and the state function's own status give ONE answer in every regime. A terminal projection on a running level is the SubFlow completion window (the child stamped its C/F upward at its rest point while this level's correlation is still open and this level is still resuming) - the columns-only form of the state function's subFlowIsTerminal guard. A terminal own status with a stale non-terminal projection is a cancel or fault cascade, which the write side cannot repair: it completes the level while the child's correlation is still open so the resync no-ops, and cleanup closes the correlation afterwards with nothing left to restamp the column. The clamp deliberately does not test HasActiveSubFlow: the list query does not include child correlations, so that predicate would be false for every list item. Instance queries may also filter and sort on effectiveStatus (names or codes, Active/A like status), but a filter runs in SQL against the STORED column and therefore does not see the clamp - effectiveStatus eq Completed can match a parent whose served effectiveStatus reads Busy; filter on status for 'is this flow done?'. The rule/role-grant script context carries it too (ExpressoInstanceView.EffectiveStatus as a code, $.context.Instance.EffectiveStatus rendered like Status). Purely additive: no field changed or was removed, the state function body is untouched and ResponseShapeVersion stays v9.",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}",
        "GET {domain}/workflows/{workflow}/instances",
        "POST {domain}/workflows/{workflow}/instances/start",
        "PATCH {domain}/workflows/{workflow}/instances/{instance}/transitions/{transition}"
      ]
    },
    "pythonTask": {
      "since": "0.0.80",
      "status": "experimental",
      "description": "Built-in trusted-code PythonTask (TaskType=23, JSON discriminator '23', Execution route 'python') invokes CPython through one explicit mode: pythonNet, process, or container. PythonTask does not execute Orchestration InputHandler/OutputHandler mappings. The only script contract is main(input), with config.input and the return value transported only as strict JSON; NAT and B64 ScriptCode are accepted while REF and filesystem loading are rejected. Disabled or unavailable modes never fall back. Python.NET is in-process and not a security sandbox; process mode uses an isolated interpreter plus Linux resource limits; container mode creates and removes one hardened runner container per invocation through an explicit Docker Engine or Kubernetes Job/attach driver and is opt-in. Runtime package installation is disabled and both standard runtimes and the multi-arch runner image consume the same transitive hash lock. AllowedModules is a governance policy rather than a security boundary.",
      "schemaPath": "task.type=23",
      "relatedConfig": [
        "Python:Enabled",
        "Python:DefaultMode",
        "Python:EnabledModes",
        "Python:MaxTimeoutSeconds",
        "Python:MaxCodeBytes",
        "Python:MaxInputBytes",
        "Python:MaxOutputBytes",
        "Python:MaxStdoutBytes",
        "Python:MaxStderrBytes",
        "Python:AllowedModules",
        "Python:PythonNet",
        "Python:Process",
        "Python:Container"
      ]
    },
    "customScriptHelpers": {
      "since": "0.0.58",
      "status": "experimental",
      "configFlag": "Scripting:Helpers:Enabled",
      "description": "Reference reusable C# helper components (sys-mappings) from a mapping via mapping.scripts.helpers[] (Reference: key/version/domain/flow), with a per-mapping mapping.scripts.allowedAssemblies grant. A workflow may also declare a flow-level scripts object that is unioned with each mapping's scripts (helpers deduped, assemblies merged). The referenced helper set is compiled first under a best-effort compile-time sandbox (reference allow-list + mandatory banned-namespace analyzer), cached by content hash, then the mapping is compiled against it with helper namespaces auto-imported. A mapping may set encoding=REF to run a sys-mappings component directly (code is a Reference, resolved from the component store; single level, no REF chaining). Grants merge on top of the Scripting:Sandbox:AllowedAssemblies baseline. Disabled by default; not a hard security boundary.",
      "relatedConfig": [
        "Scripting:Helpers:Enabled",
        "Scripting:Sandbox:Enabled",
        "Scripting:Sandbox:AllowedAssemblies",
        "Scripting:Sandbox:BannedNamespaces",
        "Scripting:Sandbox:PluginDirectory",
        "Scripting:Sandbox:AllowUnsafe"
      ]
    },
    "longPollInteractionRule": {
      "since": "0.0.92",
      "status": "stable",
      "description": "Rule-based authorization arm for interaction.longPoll (issue #936): instead of roles, a state may declare interaction.longPoll.rule - ONE condition script (scriptCode slot, IConditionMapping contract, same shape as view/notification rules) compiled and cached by the shared script engine. The two arms are mutually exclusive: WorkflowValidator rejects a longPoll declaring both roles (non-empty) and rule, validates the rule's script body like every compilable slot, and (new) validates longPoll.roles dynamic-grant syntax; the vnext-schema longPoll contract enforces exactly one of roles|rule at authoring time. Both enforcement surfaces admit through one gate (ILongPollInteractionGate, which owns the arm selection: rule, else roles, else allow): the State function emits the interaction object only when the rule returns true, and the acknowledge endpoint (POST .../longpoll/ack) rejects with 403 when it does not - so which clients see and may acknowledge a Human Task interaction can be driven by request headers, query parameters or instance data (e.g. admit when header x-channel=mobile). Instance data is read via context.Instance.Data (lazy); context.Body is deliberately NOT populated on this surface, unlike view-rule contexts. Fail-closed: a rule returning false, throwing or failing to compile denies on both surfaces; the fallbackTimeoutSeconds job still auto-resumes the paused pipeline, so a broken rule cannot strand an instance. Caching posture: a state body whose interaction is rule-gated is never stored in the shared state body cache (CallerScopeHash does not cover rule inputs; bubbled subflow interactions skip caching conservatively); the fingerprint 304 fast path is unchanged, leaving an accepted #864-class gap where rule-input changes behind an unchanged fingerprint keep answering 304 with the previous verdict until the next state/status change - the acknowledge always evaluates fresh. No state-body shape change (ResponseShapeVersion untouched).",
      "schemaPath": "state.interaction.longPoll.rule",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state",
        "POST {domain}/workflows/{workflow}/instances/{instance}/longpoll/ack"
      ]
    },
    "longPollTermination": {
      "since": "0.0.62",
      "status": "stable",
      "description": "Declarative long-poll termination on state entry. A state may declare interaction.longPoll.terminate=true (with optional interaction.longPoll.roles[] role grants and interaction.longPoll.fallbackTimeoutSeconds, default 60). When the pipeline enters such a state it runs ChangeState + OnEntry, then pauses before the epilogue (Schedule/Auto/Finish); the instance stays Busy. The State (long-poll) function returns HTTP 200 with an interaction object { terminateLongPoll: true, ack: { href } } for callers whose role is granted, telling the client to stop polling and render the entered-state screen. The client then POSTs to the acknowledge endpoint (instances/{instance}/longpoll/ack), which resumes the pipeline from where it paused; a fallback schedule auto-resumes if the client never acknowledges. The 'interaction' object is a generic, extensible container for client-workflow-manager directives.",
      "schemaPath": "state.interaction.longPoll",
      "apiEndpoints": [
        "POST {domain}/workflows/{workflow}/instances/{instance}/longpoll/ack"
      ]
    },
    "stateCorrelationHistory": {
      "since": "0.0.77",
      "status": "stable",
      "description": "The State (long-poll) function returns the full child-correlation set, not just the open ones. The existing activeCorrelations field keeps its active-only semantics, and a new correlations field carries every correlation — active and completed — ordered by createdAt ascending. Each entry adds isCompleted, completedAt, terminalOutcome (completed/faulted/canceled), currentState and stateChangedAt, so a client workflow manager can reconstruct which sub items ran and how each one ended without extra queries. Both lists merge the active subflow's corresponding list when the parent is inside a subflow. The completed rows come from a dedicated read because the instance aggregate is loaded with an active-only filtered include; correlation-set changes (a sub item starting, terminating, being reverted, or advancing its own state) now participate in the state fingerprint ETag, so a long-polling client is no longer served 304 while the set changes underneath it.",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state"
      ]
    },
    "stateScheduledTransitions": {
      "since": "0.0.80",
      "status": "stable",
      "description": "The State (long-poll) function exposes the transitions the runtime has armed to fire automatically inside the existing transitions array, as kind:'scheduled' entries of { name, kind, executeAtUtc } appended after the caller-triggerable entries and ordered by execution time ascending. Clients discriminate on kind: scheduled entries carry executeAtUtc and no href/view/schema (they cannot be triggered by callers); every other kind carries an href and never an executeAtUtc. Entries are built from the persisted job state: active InstanceJob rows of type ScheduledTransition whose ExecuteAt was captured at scheduling time from the same instant the Dapr scheduler was armed with, never a timer-script re-evaluation. executeAtUtc is an ISO 8601 UTC instant with the Z designator. Scheduled entries are not role-filtered and always describe the polled instance itself, without merging an active subflow's schedule. Scheduled-job set changes deliberately do NOT participate in the state fingerprint ETag (team decision): a same-state re-arm, an inline chain returning to the same state, or a fired job rejected under a lock conflict can leave the entries stale behind a 304 until the next state/status change - documented as a known, accepted gap. Rows persisted by runtimes before the ExecuteAt column existed are omitted until they age out. The workflow-level timeout job also records its ExecuteAt but is not listed yet.",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state"
      ],
      "schemaPath": "transitions[kind=scheduled]"
    },
    "stateNotification": {
      "since": "0.0.63",
      "status": "stable",
      "description": "Declarative state-level notifications. A state may declare a notifications[] array; each entry has a type (default 'state' — only 'state' is processed today, 'command' is reserved), an optional rule (compiled to IConditionMapping), and a mapping (compiled to IStateNotificationMapping, same contract as the NotificationTask state channel). After the transition pipeline settles and the instance state/status is finalized and committed, the engine schedules a durable one-shot background job (handler 'state.notify') that rebuilds a ScriptContext from the carried request context (Headers, RouteValues, Body), evaluates each entry's rule (absent rule => always processed; present rule => only entries whose rule returns true), and dispatches the applicable entries through the platform-managed state Dapr binding (vnext-notification-state) off the request thread, so the client response is never blocked. Fires once per settle (not on intermediate auto-chain hops). The state-channel payload is the slim pointer { domain, flow, id, version } — consumers re-fetch full state via the State function. Independent of and backward-compatible with the existing NotificationTask state channel.",
      "schemaPath": "state.notifications[]"
    },
    "scriptRelatedInstanceAccess": {
      "since": "0.0.78",
      "status": "stable",
      "description": "Mapping scripts read a related instance's data via context.Related (IRelatedInstanceAccessor) instead of duplicating it across the parent/child boundary: HasParent (sync), ParentAsync() for the one hop up to the parent that started this instance as a SubFlow/SubProcess, and SubAsync(subFlowKey)/SubsAsync(subFlowKey?)/SubKeysAsync() for one hop down to this instance's own correlations (active and completed). Keys match InstanceCorrelation.SubFlowName; SubAsync returns the newest correlation by CreatedAt, SubsAsync returns all oldest-first in one batched read. Nothing is pre-fetched; results are memoized for the ScriptContext's lifetime and shared across parallel task branches. Reads use the engine's own system identity and are unfiltered by design: no query-role check, no x-roles field filtering, no extensions, no data-function cache, so a script's decision stays deterministic across users and works in scheduled/automatic/event/job contexts with no caller identity. Copying a related field into the current instance's data does not carry its x-roles protection with it. Same-domain reads dispatch in-process; cross-domain reads call two internal-only endpoints that carry no in-app authorization and rely entirely on network isolation (Dapr sidecar-to-sidecar only). Absence resolves to null/empty; a read failure or exceeding the per-context resolution cap throws RelatedInstanceAccessException.",
      "relatedConfig": [
        "Workflow:Scripting:RelatedAccess:MaxResolutionsPerContext"
      ],
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/internal/related-data",
        "POST {domain}/workflows/{workflow}/internal/related-data/batch"
      ]
    },
    "scriptSecretCache": {
      "since": "0.0.80",
      "status": "stable",
      "configFlag": "Scripting:SecretCache:Enabled",
      "description": "Script secret functions (GetSecret/GetSecretAsync/GetSecrets/GetSecretsAsync on ScriptBase) read through a process-wide, short-TTL, in-memory cache of Dapr secret bundles instead of hitting the vault on every call. The cache key is the (storeName, secretStore) pair and the cached value is the whole bundle, so one vault round-trip serves every key in the bundle for the TTL window (default 30 seconds). Concurrent readers of the same bundle collapse into a single vault fetch (single-flight); a failed fetch is never cached, so the next call retries immediately. The cache is deliberately in-process rather than distributed: secret material never transits Redis or the wire. Enabled by default; Scripting:SecretCache:Enabled=false or TtlSeconds<=0 bypasses it entirely. After a secret rotation a process may serve the previous value for at most TtlSeconds seconds.",
      "relatedConfig": [
        "Scripting:SecretCache:Enabled",
        "Scripting:SecretCache:TtlSeconds"
      ]
    },
    "stateStoreTask": {
      "since": "0.0.64",
      "status": "stable",
      "description": "Built-in StateStoreTask (TaskType=17, discriminator 'statestore') that reads and writes a Dapr state store component from the workflow/function pipeline. When the task config omits storeName, the store is resolved from the executing runtime's DAPR_STATE_STORE_NAME configuration value, so each runtime targets its own component with no hard-coded name. Runs through the Execution API like the other Dapr tasks: the StateStoreTaskExecutor performs input/output mapping and delegates a TaskEnvelope to the StateStoreTaskInvoker, which uses DaprClient. Command names mirror the Dapr state API verbs: get (GetStateAndETagAsync; a cache miss returns success with metadata Found=false and null data, so the error boundary is not tripped), set (SaveStateAsync / TrySaveStateAsync when an ETag is supplied, with optional ttlInSeconds TTL, and StateOptions concurrency FirstWrite/LastWrite + consistency Eventual/Strong), and delete (single key via DeleteStateAsync, key list via DeleteBulkStateAsync, or tag/pattern via the Dapr state Query API which requires a query-capable state store). An explicit storeName must be exposed by the Execution sidecar; the repo ships a 'vnext-state' component there for that purpose. All task-supplied keys are stored under the fixed 'custom:' prefix to namespace task-written entries away from engine-owned cache keys sharing the same store.",
      "schemaPath": "task.type=17"
    },
    "viewDisplayModes": {
      "since": "0.0.79",
      "status": "stable",
      "description": "A view declares its display per client interface mode. attributes.display accepts the existing bare string (full-page | popup | bottom-sheet | top-sheet | drawer | inline), which continues to mean the SDI (single-document interface) display, or an object { sdi, mdi } where mdi carries the multi-document display (tab | window | split | inline) for clients that render several documents side by side. At least one mode must be present. The runtime keeps the legacy string in the view response's display field, so clients predating MDI support are unaffected, and adds a modes object carrying both values; a view declaring only mdi returns an empty display. Component JSON round-trips in the shape it was authored: an SDI-only declaration is written back as a bare string, anything declaring mdi as an object. Values are a documented vocabulary (ViewDisplayMode), not an enforced enum — the runtime accepts any non-blank value, the same way renderer is treated.",
      "schemaPath": "view.attributes.display",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/view"
      ]
    },
    "functionContract": {
      "since": "0.0.79",
      "status": "stable",
      "description": "A function declares the contract clients need in order to call it: attributes.verbs[] lists the supported HTTP verbs (GET, POST, PATCH, DELETE), and inputSchema/outputSchema (sys-schemas) plus inputView/outputView (sys-views) describe its payloads and screens. Each of the four contract slots is rule-based: author it as a single component reference, as an array of entries ([{rule, view|schema, loadData?}]), or as the wrapped array form ({views:[...]} / {schemas:[...]}). Entries are evaluated in declaration order against an IConditionMapping rule and the first match wins; an entry without a rule always matches and must therefore be the last entry. When every entry carries a rule and none matches, no contract applies for that request - this is not an error. All gates are opt-in: a function declaring no verbs accepts every routed verb, and one declaring no inputSchema accepts any body, so definitions authored before contract declaration are unaffected. When verbs are declared, an undeclared verb is rejected with HTTP 405 and an Allow response header; when inputSchema resolves, a request that carries a body is validated against it before any task runs, using the same JSON-schema validator and localization as transition schema validation. outputSchema is declarative only and is not enforced. Component validation rejects an unknown verb, a reference pointing at the wrong flow, an inputSchema declared alongside verbs that can never carry a body, a rule-less entry that is not last (everything after it is unreachable), and an 'extensions' declaration on a function view entry (functions have no data function to apply extensions to). The script context used for rule evaluation is built at most once per request and only when some entry declares a rule. The HTTP QUERY method is deliberately not supported yet - .NET 10 exposes it at the HTTP layer (HttpMethods.Query) and MVC's HttpMethodAttribute makes routing it trivial, but Swagger/OpenAPI generation, gateways and client SDKs do not handle an unrecognised method, so body-carrying reads should be modelled as POST for now.",
      "schemaPath": "function.attributes.verbs, function.attributes.inputSchema, function.attributes.outputSchema, function.attributes.inputView, function.attributes.outputView"
    },
    "functionInfoEndpoint": {
      "since": "0.0.79",
      "status": "stable",
      "description": "Function discovery. GET {domain}/functions/{function}/info and GET {domain}/workflows/{workflow}/instances/{instance}/functions/{function}/info describe a custom function to a client that is about to call it, in the state function's hyperlink style: key/domain/version, the scope code, rawResponse and cacheable flags, a 'function' href carrying the accepted verbs, and inputView/outputView/inputSchema/outputSchema hrefs each with a hasView/hasSchema flag. The flags report whether following the href right now returns content; the href is emitted either way, because a rule reads request state and may match on a later call. The hrefs point at four content routes - {...}/functions/{function}/view?target=input|output and {...}/functions/{function}/schema?target=input|output, in both domain and instance scope - which re-evaluate the contract rules on every call and return the same view payload as the state-level view function. target defaults to 'input'. All six routes are GET-only and carry no ETag/304, and all six run the same scope and role gates as execution (IFunctionAccessPolicy), so a caller who could not invoke the function receives 403 rather than a description of it. A slot that resolves to nothing on a content route returns 404 (Function:800004); an unrecognised target returns 400 (Function:800005). Built-in system functions (state, view, data, schema, authorize, permissions, hierarchy, human-task, master, catalog) have no sys-functions component and return 404 from /info. Function discovery from a polling client goes through the built-in 'catalog' function: the state response carries functions.hasFunctions (workflow.Functions.Count > 0) plus functions.href pointing at GET {domain}/workflows/{workflow}/instances/{instance}/functions/catalog, and the catalog returns { functions: [ { name, version, scope, href } ] } in declaration order. Each href is built from the function's declared scope - scope D links to the domain info route, scope F and I to the instance info route, because the domain route rejects non-Domain scopes with 403 - and the scope code travels alongside so clients can branch on it. The catalog is role-filtered through the same IFunctionAccessPolicy as execution and /info, so a function the caller could not invoke is not advertised and every link handed out is actionable; a reference whose component cannot be resolved is logged and omitted rather than failing the catalog. The list is deliberately NOT inlined in the state response: resolving it costs one component read per declared function plus a role evaluation each, which does not belong on a response served on every long-poll and is wasted for clients that never look at functions - the state path reads no function components at all. functions.hasFunctions is excluded from the state ETag material because it is a property of the flow version, which the instance fingerprint already covers, so it cannot change while an instance is parked; StateFunctionCache.ResponseShapeVersion was bumped to v4 instead, which is what makes existing long-polling clients observe the new shape."
    },
    "wellKnownTransitionDiscovery": {
      "since": "0.0.79",
      "status": "stable",
      "description": "The workflow-level well-known transitions updateData and exit are listed in the State function's availableTransitions, alongside cancel which was already listed, so the Client Workflow Manager SDK drives them through the same loop it uses for state and shared transitions instead of hard-coding the well-known keys. Each is listed when it is configured, declared with triggerType manual (0) or event (3), and permitted in the current state by availableIn (empty/absent means every state, same semantics as shared transitions); while the instance sits in a SubFlow state the parent's cancel, updateData and exit are merged into the subflow's list, which is updateData's primary surface since its preflight step only acts in that situation. The listed value is the configured key, never the well-known alias (cancel | update-parent-data | exit), because role filtering resolves keys through FindTransitionInContext, which matches these three on their configured key; the aliases remain accepted on the request side. Each entry carries a kind discriminator mirroring the workflow-definition field names — cancel | updateData | exit — so kind is updateData, not the update-parent-data alias, and view/schema/annotations resolve from the transition definition exactly as for a state transition. roles on all three are now enforced at discovery time: availableTransitions is filtered per caller with the same RoleGrant evaluation as any other transition (DENY wins, any ALLOW makes the set an allowlist, a deny-only set is a blacklist, predefined and dynamic roles apply), and /functions/authorize plus the authorization matrix evaluate them; while in a subflow, parent-owned keys are filtered against the parent's grants. Role grants are not re-checked at POST .../transitions/{key}, which is the pre-existing behavior for every transition type rather than a gap specific to these three. WorkflowValidator now runs updateData and exit through the same single-transition validation as cancel, covering dynamic role-grant syntax and trigger-type rules; updateData.target must still be $self. Role-grant validation itself was previously unreachable: it gated on DynamicRoleGrant.IsDynamicRole, which returns false for a malformed remainder and therefore skipped exactly the grants the rule exists to reject, and it compared the $.context. literal with OrdinalIgnoreCase while the runtime parser uses Ordinal. Classification now lives in DynamicRoleGrant.Classify, sharing TryParse's prefix constants and comparisons, so a grant declaring dynamic-role intent via a $user. / $userBehalfOf. / $role. qualifier must be followed by the literal case-sensitive $.context. and a non-empty navigation path — $user.customer, $user.$.Context.ownerId and $role.$.context. are all definition-time errors instead of silently degrading to static role names that can never match. Static role names and the four predefined instance roles remain free-form and unvalidated. Schema: cancelTransition, exitTransition and updateDataTransition already accepted roles as roleGrant arrays; availableIn was added to all three in this release to close a schema/runtime gap — the runtime had always honored availableIn on these transitions (and the capability matrix documented it as optional) but the definitions omitted the field while declaring additionalProperties: false, so authoring it was rejected and the gate was unreachable.",
      "schemaPath": "workflow.attributes.exit.roles",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/authorize",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/authorization-matrix"
      ]
    },
    "roleGrantAuthorization": {
      "since": "0.0.79",
      "status": "stable",
      "description": "All role grant evaluation funnels through a single batch-scoped evaluator (IRoleGrantEvaluator, created via ITransitionAuthorizationManager.CreateEvaluatorAsync), so every authorization surface applies the identical rule: DENY wins over the whole grant set; with no matching DENY any matching ALLOW allows; a set with no ALLOW grant is a blacklist (allowed unless explicitly denied); an empty set is allowed. Multi-role callers resolve as any-allowed-role-grants-access, and a caller with no roles is still evaluated once so predefined and dynamic grants apply. Surfaces covered: custom function 'roles' (403 on deny), state/workflow 'queryRoles' for the built-in state/view/data/schema functions (403), transition 'roles' for availableTransitions discovery, the authorize function, the human-task list, long-poll acknowledge roles (403), and schema field-level 'x-roles'. Transition execution (POST instances/{instance}/transitions/{key}) deliberately does NOT evaluate transition 'roles': roles are a discovery control describing what a client should offer, not a capability boundary, and TransitionExecutionPolicy contains no role specification. Callers needing a pre-flight decision use the authorize function. Predefined and dynamic grants are matched on the grant side rather than by synthesizing pseudo-roles into the caller's role list, so schema 'x-roles' now honors $InstanceStarter/$PreviousUser/$InstanceBehalfOfStarter/$PreviousBehalfOfUser and $user./$userBehalfOf./$role. dynamic references at runtime. Predefined actor roles resolve ICurrentUser.ActorUserName against Instance.CreatedBy; behalf-of roles resolve ICurrentUser.UserName against Instance.CreatedByBehalfOf. The evaluator resolves the previous manual transition at most once per batch and memoizes the dynamic-role authorization context per transition key, built lazily so a grant set without dynamic grants never serializes instance data.",
      "schemaPath": "transition.roles | function.attributes.roles | workflow.queryRoles | states[].queryRoles | schema.properties[].x-roles",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/authorize",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/data",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/schema",
        "GET {domain}/functions/human-task"
      ]
    },
    "availableInRoleScoping": {
      "since": "0.0.80",
      "status": "stable",
      "description": "availableIn is extended from a plain array of state keys to an array whose items are either a bare state key or an object { state, roles }, and the two forms may be mixed in one array. It is optional on sharedTransition (manual only) and on the well-known cancelTransition, exitTransition and updateDataTransition. An entry carrying no roles is exactly equivalent to the bare string form, so definitions authored earlier behave identically and this is not a breaking change; AvailableInJsonConverter derives the written shape from whether an entry carries roles rather than remembering how it was authored: the bare-string form and the roles-bearing object form round-trip byte-for-byte, while a role-less object ({state} or {state, roles: []}) is normalized to the equivalent bare string. That normalization is lossless because the two spellings encode the same thing, and it matches ViewDisplayJsonConverter, which likewise writes an SDI-only display back as a bare string instead of an object. Per-state roles compose with the transition's own roles as an AND: transition.roles is the global gate and the matching entry's roles narrow it for that one state, so a caller must satisfy both. Each level is evaluated independently by the shared IRoleGrantEvaluator, so DENY-wins, allowlist-versus-blacklist and predefined/dynamic role resolution behave identically at both levels, and an empty grant set allows. Enforcement is now consistent across the three surfaces that previously disagreed: the State function filters availableTransitions by state and by both role levels; the authorize function evaluates state and both role levels, where before it ignored the instance's current state entirely and could answer allowed for a transition the execution policy then rejected; and the transition execution policy gates on state only, because role grants have never been re-checked at execution for any transition type. Both role-aware surfaces funnel through ITransitionAuthorizationManager.IsTransitionAllowedInStateAsync and FilterAuthorizedTransitionKeysAsync so they cannot diverge. The execution-side state gate is new for the well-known three: WellKnownTransitionSpecification previously returned success unconditionally while StateTransitionListSpecification excluded those keys, so cancel, updateData and exit could be executed from any state regardless of availableIn; they are now rejected with Transition:100024, and the specification claims keys via Workflow.IsWellKnownTransitionKey, which matches reserved aliases as well as configured custom keys. WorkflowValidator validates availableIn on shared and well-known transitions: every entry must name an existing state, no state may be listed twice (FindAvailableIn takes the first match, so a duplicate is silently dead), and each entry's roles goes through the same dynamic-role syntax check as any other grant set. The authorization-matrix response now reports availableIn per transition; it stays state-independent by design. Monitoring gained the state filter it was missing for shared transitions and its lone OrdinalIgnoreCase state comparison was aligned with the runtime's Ordinal one.",
      "schemaPath": "workflow.attributes.sharedTransitions[].availableIn",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/authorize",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/authorization-matrix",
        "POST {domain}/workflows/{workflow}/instances/{instance}/transitions/{transitionKey}"
      ]
    },
    "selfTargetPipelineProfile": {
      "since": "0.0.80",
      "status": "stable",
      "description": "An updateData transition runs under a self-target pipeline profile composed on top of the trigger's profile (Manual+Self, AutoChain+Self, ...). Because updateData writes data without moving the instance - its target is fixed to the reserved $self key by the validator - the state's lifecycle steps are skipped: CancelScheduledJobs (39), OnExit (40), OnEntry (60) and Schedule (80). ChangeState (50) still runs, since it is the only step that sets the target the auto step reads, and OnExecute (30) still runs, being the transition's own work rather than the state's lifecycle. The practical effect is that updateData writes its data and evaluates the state's auto transitions against it without re-running the state's entry hooks or restarting its timers. updateData is the ONLY transition this applies to. The variant is named for the target, but the selection is a policy (PipelineProfileResolver, TransitionExecutionContextExtensions.SkipsStateLifecycle): any other transition declaring target $self - a shared transition being the real case - keeps the trigger's base profile and runs the state's FULL lifecycle, so its OnExit/OnEntry fire and its scheduled transitions are cancelled and re-armed. Declaring $self states that the instance must not move, not that the state's hooks should be skipped; a frequently invoked $self shared transition on a short-timeout state therefore defers that timeout on every call. ChangeStateStep's suppression of the state-change metric, log and span event is scoped the same way, so a $self shared transition still reports its state change. For the target check itself only the authored $self keyword qualifies: a literal target that happens to equal the current state does NOT, because the start transition (instance pre-positioned into the initial state at creation) and a retry after the state change already committed both present that same shape while genuinely needing the state entered. A parent with an open SubFlow correlation continues to short-circuit earlier, at HandleUpdateDataDataOnlyStep (21), to a data-only update. Timeout and subflow-resume executions are excluded from the detection, since their target does not come from transition.target.",
      "schemaPath": "workflow.attributes.updateData.target",
      "apiEndpoints": [
        "POST {domain}/workflows/{workflow}/instances/{instance}/transitions/{transitionKey}"
      ]
    },
    "fanOutTask": {
      "since": "0.0.80",
      "status": "stable",
      "description": "Built-in FanOutTask (TaskType=21, discriminator 'fanout') resolves a collection from instance data at runtime (itemsPath, a dot-path JSONPath subset, or the mapping's IFanOutMapping.ItemSelector - exactly one of the two) and runs a referenced inner task once per item, in parallel, then joins the per-item outcomes into ONE task result and ONE instance-data write. Phase 1 supports inline mode only; mode='durable' is present in the schema but rejected at parse time, reserved for a later phase. Concurrency is bounded at two levels: execution.maxDegreeOfParallelism (per-batch SemaphoreSlim, default 4) and a process-wide global bulkhead sized by Workflow:FanOut:MaxConcurrentItems (default 64, FanOutConcurrencyLimiter) shared by every fan-out batch in the process - effective concurrency is min(batch's remaining slots, global remaining slots), which is what stops N concurrently running instances from multiplying into N x maxDegreeOfParallelism simultaneous downstream calls. join.policy selects how per-item outcomes combine: 'all' (every item must succeed and not time out, cancels the rest on first failure), 'allSettled' (always succeeds - the expected common case, branch on the result summary via auto-transitions), 'quorum' (succeeded >= join.minSuccess), 'firstSuccess' (succeeded >= 1, cancels the rest on first success - definitionally quorum with minSuccess=1). An empty collection succeeds for all/allSettled (vacuously) and fails for quorum/firstSuccess (zero can never clear a threshold of at least one - the two must never diverge on this). Nested fan-out (an inner task that itself resolves to TaskType=21) is rejected at runtime because a nested batch would deadlock against the same global bulkhead. Each item runs through the full task engine (its own retry, its own per-item errorBoundary, its own InstanceTask journal row keyed '{fanOutTaskKey}#{index}') with TaskEngineExecutionOptions.SuppressDataApply=true, on an isolated ScriptContext.CreateParallelBranch() and its own DI scope, and that branch is discarded rather than merged back - this is what makes IFanOutMapping.ItemInputHandler safe to run N times in parallel: it mutates the cloned inner task and must be pure with respect to instance data. IFanOutMapping.OutputHandler is the single write point, called exactly once per batch; with no mapping configured, the executor's default binding sets the branch body to the raw item value regardless of itemAlias (itemAlias never participates in input binding; it is a reporting label surfaced as a structured field on the FanOutBatchStarted log line and as a tag on every item's trace span) and the default output writes item results under join.resultKey plus a {resultKey}Summary counters object. Public error codes for partial-failure branching: FanOut:ItemTimeout, FanOut:BatchTimeout, FanOut:ItemCancelled, FanOut:ItemNotStarted, FanOut:ItemFailed. The executor and its DI registration (FanOutTaskExecutor, FanOutOptions, FanOutConcurrencyLimiter) are orchestration-local only - the Execution host never registers them, since FanOut has no remote invoker of its own; remote inner task types still cross to Execution through their own existing invokers.",
      "schemaPath": "task.type=21"
    },
    "externalHttpTask": {
      "since": "0.0.81",
      "status": "stable",
      "description": "New workflow task type 22 (ExternalHttp): a user-defined URL call executed directly by the Orchestrator process instead of being routed through the Execution service's /execution/invoke/{type}/{key} hop. The task definition shape is identical to the type-6 HTTP task (config.url, method, headers, body, contentType, rawBody, timeoutSeconds, validateSsl, acceptedStatusCodes), and ExternalHttpTask derives from HttpTask so mapping scripts (task as HttpTask, SetUrl/SetHeaders/SetBody) work unchanged. The orchestrator flattens the task through the same TaskBindingMapper as the remote path and performs the call in-process with the same named HTTP clients, header/Content-Type semantics, response parsing and accepted-status-code matching, so output mappings observe identical result shapes for both types. Note: no Dapr sidecar circuit breaker or remote-invocation timeout participates; the task's own timeoutSeconds (default 30) is the only bound below the job budget.",
      "schemaPath": "task.type=22"
    },
    "stateIncident": {
      "since": "0.0.89",
      "status": "stable",
      "description": "Incidents moved out of the Instances.Incidents jsonb column into their own InstanceIncidents table (one row per incident, unbounded history, cascade-deleted with the instance) with a denormalized Instances.HasActiveIncident flag (partial index on true). The State (long-poll) function body always carries an incident block, and it carries LINKS rather than content: { hasActiveIncident, active: { href }, history: { href } }. active is present only while hasActiveIncident is true, so a client follows it exactly when there is something to fetch; history is always present. The identical block is metadata.incident on GET .../instances/{instance} and on every item of the list view, so a client learns one shape. active.href targets GET .../instances/{instance}/incidents/active, which returns the newest unresolved incident and answers 404 (Instance:100037) when none is open — a normal outcome, since a successful retry resolves the incident between a poll and the follow-up read; a caller failing the role gate gets 403 instead, so 'no incident' stays distinguishable from 'not allowed to know'. No surface returns a stack trace. When the instance delegates to an active subflow, active.href addresses the subflow that OWNS the incident while history.href always targets the polled instance. Visible to every caller who passes the state function's queryRoles gate. hasActiveIncident participates in the state fingerprint ETag, so raising or resolving an incident without a state or status change (error-boundary Abort with a transition, FinalizeTransitionStep resolve) still invalidates a parked long-poller's 304; the former stale-active gap (resolving one incident and raising another inside a single parked state) is GONE rather than merely narrower: the body no longer carries incident content, so there is nothing left to go stale and the client re-fetches through active.href. A failure records the incident before the task step's own save, so the row and the flag commit together and the pipeline's fault path adds no duplicate: active on a faulted instance is the boundary's own verdict and carries boundaryAction whenever a rule resolved one. On a rollback/notify outcome that means a brief hasActiveIncident=true window while the instance is Busy, before FinalizeTransitionStep closes the row. Unfault (successful retry) and a boundary transition completing without a new fault resolve every open incident and recompute the flag, so a recovered instance reports no active incident; retryCount is always 0 and ignore/log record no incident. ResponseShapeVersion is v9 (v8 was the embedded-summary shape, never released). history.href points at GET {domain}/workflows/{workflow}/instances/{instance}/incidents, which pages the full history newest-first (items, page, pageSize <= 100, hasNext, hasActiveIncident) under the same queryRoles gate. The Monitor incidents endpoint pages the same table (pageSize <= 200) and keeps stack traces for operators.",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/state",
        "GET {domain}/workflows/{workflow}/instances/{instance}/incidents",
        "GET {domain}/workflows/{workflow}/instances/{instance}/incidents/active",
        "GET {domain}/workflows/{workflow}/instances/{instance}",
        "GET {domain}/workflows/{workflow}/instances"
      ],
      "schemaPath": "incident"
    },
    "taskAndActionHistory": {
      "since": "0.0.90",
      "status": "stable",
      "description": "tasks and actions system functions over the task journal (issue #939), dispatched like state/data/hierarchy via the {function} route segment (IInstanceFunctionHandler registrations, keys in FunctionTypeConst - a custom function named tasks or actions is shadowed, the standard system-function-key rule). GET .../instances/{instance}/functions/tasks returns the instance's FULL InstanceTasks journal in execution order (StartedAt ascending, unpaged) under the same queryRoles gate as the state function. Each row carries execution METADATA ONLY: journal row id, taskKey, the owning transition's context (transitionKey, fromState, toState, triggerType), status (waiting/busy/completed/faulted), businessStatus (unknown/success/failed), startedAt/finishedAt/durationMs, and error - the fault reason extracted from the journaled {\"error\":...} response of a faulted task. The journaled request/response/invocationResult payloads are deliberately NOT exposed: mapping scripts write their built headers (including auth material) into them, so full payloads are served by NO API since the Monitor host removal (#982, monitor-api-host deprecation) - they stay in the journal table, operator/DB material only; the repository read projects the metadata columns in SQL (InstanceTaskHistoryRow), so the jsonb payloads never even leave the database (the Faulted rows' small {\"error\":...} Response is the one CASE-fetched exception). GET .../functions/actions?taskId={id} returns the recorded execution sub-steps (InstanceActions) of one journal row in execution order, answers 400 (Instance:100039) without a valid taskId query parameter and 404 (Instance:100038) when the task does not belong to the instance in the route, and echoes the owning taskId/taskKey. KNOWN GAP: nothing in the runtime writes InstanceAction rows yet - the function contract is in place and returns an empty list until a writer lands (tracked as a follow-up to #939). Neither function touches the state function body, so no ResponseShapeVersion change.",
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/tasks",
        "GET {domain}/workflows/{workflow}/instances/{instance}/functions/actions?taskId={taskId}"
      ]
    },
    "instanceQueryPaging": {
      "since": "0.0.90",
      "status": "stable",
      "description": "Instance lists apply sorting and pagination in PostgreSQL. Non-grouped, non-aggregate requests select pageSize + 1 identities to determine hasNext, hydrate only the requested page and preserve the selected order. An instance Id tie-breaker makes equal sort values deterministic; this is offset pagination, not a snapshot across concurrent writes. InstanceQueries:IdentityPaging and InstanceQueries:LatestJoin default to true. DisabledSchemas contains physical schema names that use the entity-list route instead. Grouped/aggregate requests keep their separate SQL path. These routing switches do not change filter authorization or remove physical indexes.",
      "relatedConfig": [
        "InstanceQueries:IdentityPaging",
        "InstanceQueries:LatestJoin",
        "InstanceQueries:DisabledSchemas"
      ],
      "apiEndpoints": [
        "GET {domain}/workflows/{workflow}/instances"
      ],
      "documentation": "docs/runtime/instance-filtering-and-queries.md"
    },
    "masterAttributeIndexes": {
      "since": "0.0.90",
      "status": "stable",
      "configFlag": "AttributeIndexes:Enabled",
      "description": "Boolean x-indexed metadata opts Master schema scalar fields into manually prepared PostgreSQL projections. Any occurrence of x-indexed, including false, requires the exact component attributes.type value master; other schema type strings remain valid without this metadata. True supports explicit string, number, integer and boolean properties, including nested object properties and string format date-time; array, object, reference, conditional and ambiguous indexed paths are rejected. x-filterOperators and x-sortable still control filter/sort permissions independently. The sibling vnext-workflow-cli command wf indexes generate produces offline SQL for DBA review and execution in a maintenance window; publishing schemas, reading instances and starting hosts never prepare indexes or schedule preparation jobs. Routing is disabled by default. When enabled, ready projections are discovered through a distributed catalog cache scoped by database, database role and flow schema, with a distributed refresh lock and a default 30-second absolute TTL. Missing/unready projections, a missing catalog table or refresh-lock contention without a snapshot use JSON expressions. Catalog connection/read, cache and refresh-lock failures also fall back to JSON expressions and emit a warning without caching a failure as an empty snapshot. Caller cancellation and invalid schema names still propagate. This fallback does not repair database failures that also prevent the underlying instance query. Disable routing globally or by flow to return to JSON expressions; retirement of physical projections is a separate DBA operation requiring drained readers/writers and expiry of the shared catalog cache.",
      "schemaPath": "schema.attributes.schema.properties[].x-indexed",
      "relatedConfig": [
        "AttributeIndexes:Enabled",
        "AttributeIndexes:DisabledFlows",
        "AttributeIndexes:CatalogCacheSeconds"
      ],
      "documentation": "docs/runtime/manual-attribute-index-maintenance.md"
    },
    "discoveryDomainListWarmup": {
      "since": "0.0.92",
      "status": "stable",
      "description": "The discovery endpoint cache's bulk warm-up reads the registry's domain-list function - GET {ServiceDiscovery:BaseUrl}/{ServiceDiscovery:Domain}/functions/domain-list - once per refresh window, instead of paging the raw domain instance list. The function is Domain-scope, answers { items: [ { domainName, baseUrl, appId, healthUrl } ] } for the whole registry in one call, and is itself cached on the registry side for 24h under discovery:domains:active with write-through eviction on domain create/update. Nothing else about the cache's mechanics changed: the hour-long window, the distributed lock, the refresh marker, the L2 dead-man switch and the fail-open behaviour are unchanged, and an unreadable registry still leaves every lookup resolving live. The in-process L1 TTL default did move in the same release, from 60s to 600s - see the deprecations entry service-discovery-cache-l1-ttl-default. Requires a discovery registry that exposes domain-list; there is no fallback to the previous paged read.",
      "configuration": {
        "ServiceDiscovery:Cache:DomainListEndpointTemplate": "/{0}/functions/domain-list",
        "ServiceDiscovery:Cache:DomainListExpectedMax": 500,
        "ServiceDiscovery:Cache:L1TtlSeconds": 600
      }
    },
    "taskInvocationRouting": {
      "since": "0.0.92",
      "status": "stable",
      "description": "Task invocation is now mode-driven instead of hardcoded to the Execution service (issue #1007). ITaskInvocationRouter resolves, per call, first match wins: a per-task-definition override hook (config.executionMode - reserved for a future vnext-schema release, always null today, never actually decides yet), the per-type entry in Workflow:TaskInvocation:Modes (matched case-insensitively against the wire task type), the configured Workflow:TaskInvocation:DefaultMode, and finally an unconditional capability gate - Local only resolves if an ILocalTaskInvoker is registered for that wire type, otherwise the decision degrades to Remote so a misconfiguration never fails a task. The shipped appsettings.json sets DefaultMode=Remote and routes exactly five wire types Local: http (TaskType 6), daprservice (TaskType 3), soap (TaskType 16), statestore (TaskType 17) and cacheaside (TaskType 18); every other wire type (daprbinding, daprhttpendpoint, daprpubsub, daprconversation, python, the trigger/query types) has no local invoker and always resolves Remote regardless of configuration. A Local task runs in-process inside Orchestration through the same shared send/invocation cores the Execution-side invokers use (HttpTaskInvocation, StateStoreInvocation), so output shapes are identical to the Remote path by construction; a CacheAside miss re-enters the same router for its source task rather than being forced onto one host. Running locally trades away the sidecar's circuit breaker and the ExecutionApi:InvocationTimeoutSeconds (60s) layer, which has no second hop left to bound. The middle timeout layer is not lost, it is replaced: TaskInvocationDispatcher wraps every in-process call in Workflow:TaskInvocation:LocalInvocationTimeoutSeconds (default 60s) regardless of task type, so the local layering is the task's own timeoutSeconds (http/soap only, default 30) inside LocalInvocationTimeoutSeconds inside the job budget. That bound matters most for daprservice/statestore/cacheaside, whose bindings carry no timeout field at all and would otherwise wait unbounded. A local timeout surfaces as a failed result with status 408 that the error boundary can match on, never as an exception; caller cancellation still propagates. WorkflowExecutionOptionsValidator, which enforces the options-level budget hierarchy, is deliberately unchanged by this feature; a locally-run task whose timeoutSeconds meets or exceeds the job budget is not rejected at publish time, it is simply inert because the job budget fires first. ExecutionMode.Custom is rejected at options-validation time (ValidateOnStart) for both DefaultMode and every Modes entry. The resolved mode is tagged on the ambient task span as vnext.task.invocation.mode (Local|Remote) so the path a given invocation took is answerable from a trace. Reverting a type to Remote is a one-line appsettings.json change under Workflow:TaskInvocation:Modes, no code change or Execution redeploy required. The HTTP/SOAP named-client connection cap (previously hardcoded MaxConnectionsPerServer=10, written only for the type-22 External HTTP task) is now Workflow:TaskInvocation:MaxConnectionsPerServer, default 50, since it now bounds every HTTP/SOAP egress call the orchestrator performs, not just type 22's.",
      "relatedConfig": [
        "Workflow:TaskInvocation:DefaultMode",
        "Workflow:TaskInvocation:Modes",
        "Workflow:TaskInvocation:LocalInvocationTimeoutSeconds",
        "Workflow:TaskInvocation:MaxConnectionsPerServer"
      ],
      "schemaPath": "task.type=3|6|16|17|18",
      "documentation": "docs/runtime/task-invocation-routing.md"
    }
  }
}