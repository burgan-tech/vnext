# Agent Council — Decision Log

Chronological index of every council session. Session folders live under `ai-docs/agent-council/sessions/<task-id>/` (git-ignored, local to the machine that ran the council); this index is the only committed record. One row per session,
appended when `DECISION.md` is recorded (and updated if the verdict later changes). This is the
history: read it first to find out whether a question has already been decided, then open the
session folder for the evidence, dissent and conditions.

Session folders are NOT committed (team decision 2026-09-07). Keep the verdict, selected approach and follow-up in this row rich enough to stand alone;
the folder link below resolves only on the machine that ran the session.

| Date | Session | Topic | Risk | Verdict | Selected approach | Follow-up |
|------|---------|-------|------|---------|-------------------|-----------|
| 2026-09-07 | [2026-09-07-sse-push-channel](../../../ai-docs/agent-council/sessions/2026-09-07-sse-push-channel/DECISION.md) | Add SSE as a push channel for instance state changes? | High | `EXPERIMENT_REQUIRED` (Awaiting Chair) | No SSE now; poll never retired; body-over-stream and broker-ACL export rejected; transport-independent `instance-changed` pointer contract approved in principle; experiments E1–E3 + bounded ownership clarification C2 before a transport session | — |
| 2026-09-08 | [2026-09-08-substate-postcommit-relay](../../../ai-docs/agent-council/sessions/2026-09-08-substate-postcommit-relay/DECISION.md) | Post-commit direct relay for `InstanceSubStateChangedEvent` on a generic typed post-commit relay abstraction (fire-and-forget + delivery guarantee)? | High | **`APPROVED` by Chair 2026-09-09** (was: split `APPROVE_WITH_CONDITIONS` guard + mechanism flag-off · `CLARIFICATION_REQUIRED` flag-on · `EXPERIMENT_REQUIRED` default-on/detached) | (1) CAS admission gate in `SubflowStateService` (correlation row, `IsCompleted` + µs `ChangedAt`, then existing tracked write so grandparent event still emits; no schema change) ships regardless; (2) `switch` in `SubflowTerminalRelay` → typed `IPostCommitEventRelay<TEvent>` dispatcher, terminals moved verbatim, `sub:state-changed` relay via existing `UpdateSubFlowStateAsync`, awaited same-domain, awaited with 2 s bound cross-domain, depth-1, `WorkflowExecution:PostCommitRelay:SubStateChanged=false`; bare fire-and-forget and hosted channel rejected for v1; precedent rewording to per-event registration with a four-part bar | Implemented on `feature/post-commit-event-relay` with **three requester-directed deviations**, recorded as an addendum in the session's `DECISION.md`: (a) **no config flag** — opt-in is the DI registration itself, so the relay is live on merge and rollback is revert+redeploy rather than a config flip (C1 becomes an accepted risk, C3 moves to a pre-merge measurement, C4 becomes a deploy-ordering condition); (b) **depth ≥ 2 in scope** — `SubflowStateService` is a second dispatcher call site, so the fast path walks the whole ancestor chain instead of stopping at depth 1; (c) **the CAS admission gate is NOT built** — its aim (order safety) is met by giving `SubflowStateService` the same per-sub-item lock the three terminal paths already take. A correlation-first CAS was found to invert the P→C write order every terminal path uses and to deadlock (40P01) against them on the hot path. Helm pool fix guide `ai-docs/superpowers/plans/2026-09-08-dapr-redis-pubsub-pool-fix-helm-guide.md` must be deployed before the relay image ships. |
| 2026-09-08 | [2026-09-08-parent-notification-mechanism](../../../ai-docs/agent-council/sessions/2026-09-08-parent-notification-mechanism/DECISION.md) | Is a distributed event the right mechanism for a child→parent notification that drives the parent's transition lifecycle (same-domain + cross-domain, gap ~0)? | High | **Do not replace the mechanism.** Split: `APPROVED` (P0 unreleased chain reserve · P1 observability floor · P2 locked path must read `SettledAt` · P3a CAS gate · P4 phase-1 commit order · P5 stop acking recipient-less domain events) · `APPROVE_WITH_CONDITIONS` (P2 deletions, P3b causal key) · `EXPERIMENT_REQUIRED` (P3c emission coalescing, per-correlation watchdog) · `CLARIFICATION_REQUIRED` (P6 cross-domain upward retry scope) · `BLOCKED` (P7 replace the broker as terminal router; P8 sweeper in Redrive/Escalate) — Awaiting Chair | Mechanism is not the problem: 2158/2158 production relays clean, the whole phase-2 failure family 0 in 60 d, same-domain transport ≈0 ms of a ~50 ms window that is two pipelines + 3 UoWs + ≥2 COMMITs, and "gap ≈ 0" exists in one prose sentence vs a written p99 ≤ 250 ms that passes 4×. **The real find is E31: an accept-time subflow chain reserve is never released when the post-commit `ForwardToSubflowJob` fails — policy declines to fault, neither Settle nor Fault runs, and `ReleaseSubflowChainAsync` has ZERO call sites ⇒ 51 stranded production instances / 30 d, all 5 live versions, 6 on the day of the session — in the DOWNWARD command path.** Second find: `sub:state-changed` is wrong in granularity, not in kind — 6 facts per 908 ms chain onto a ~1.3 s transport (57× lag-to-churn), 7.5 % of 42 803 deliveries write nothing, ~10× the terminal channels' volume. Third: E26 — the locked terminal path drops "committed but undischarged" deliveries as duplicates without reading `SettledAt`, so the durable backup cannot recover the one failure it exists for (latent, 0 occurrences). Devil's Advocate withdrew K1 on the record; Reliability withdrew its sweeper; Pragmatic re-ranked and withdrew A1; Solution Architect withdrew S3's sunset and demoted S4. | C-01 P0 immediate; **C-02 dedicated security session with Security Guardian seated** (no `[Authorize]`, no NetworkPolicy in either repo, settlement never verifies the child's status); C-03 watchdog needs incidence + a priced Dapr arm; C-04 annotate the n=30 verification record + stale `TEST-SCENARIOS.md`; C-05 per-channel latency targets; C-06 overturn the ordering-key deferral; C-07 explain 52 `Conflict` drops. Precedent amendment requested on the dual-delivery clause. **v2 addendum — requester's late P-08 (`IWorkflowControlBus` + local `.NET Channel` + cross-domain gRPC):** split. `APPROVE_WITH_CONDITIONS` on the **receiver-side durable inbox** (new P9, vehicle for P6 — reuses Aether `IInboxStore`/`InboxProcessor`, zero migrations, `sys_queues`, escapes the five-version deprecation trap that blocked S4) and on **splitting the typed remote client along the plane boundary** (new F — `IRemoteInstanceCommandAppService`'s 12 endpoints span four semantic classes under one `Mutating` profile, which is what blocks S1/P6). **`Channel<T>` REJECTED** on nine grounds (local dispatch is already awaited and 71.7 % of gateway calls are `sync=true`; transport is ≈0 ms; first in-memory-only queue in the runtime; ~10 s real grace, no `preStop`; would have MASKED E31). Native gRPC **REJECTED**; sender-side pending-action row **BLOCKED** — the diagram's DB transaction **does not exist** (`IsTransactional` defaults false); rename **REJECTED**. Substituted **P10 `SubflowSettlementBulkhead`** (1.5–2.5 d) for the Channel's real insight, back-pressure: settlement concurrency is uncapped at ~33–50/pod and both recorded cascades were unbounded concurrency against a fixed pool. P-01 S4 formally withdrawn by its author; C-08 adopts the proposal's design contribution — name the control/fact planes in docs |
| 2026-09-11 | [2026-09-11-route-trace-coverage](../../../ai-docs/agent-council/sessions/2026-09-11-route-trace-coverage/DECISION.md) | Extend the transition pipeline's trace/span discipline to every remaining route (read path, authorization, functions, gateways, internal endpoints, workers), with per-route Elastic evidence? | High | Split, **Awaiting Chair**: `APPROVED` (repairs) · `APPROVE_WITH_CONDITIONS` (8 core span families, G-1…G-4 + RC-1…RC-11) · `CLARIFICATION_REQUIRED` (6 contested families) · `REJECTED` (screenshots, lab Elastic overlay, ES script as a merge gate, the 51/51 claim) | Scope cut ~40 span families → **8 approved + 6 conditional**, in **two** PRs sequenced behind the live preprod `effective-status-fingerprint` fix (it rewrites the same five files). Hot path settled **unanimously in the requester's favour**: zero `BBT.Workflow.*` activities created on the 304/cache-hit branch, discriminator tags on the transaction in **both** branches, gate enforced **in-process** — so nothing needs starting for this to merge. One new source `BBT.Workflow.Gateway` (4-2; `Definitions` withdrawn by its author). `Gateway.Invoke` (~24 sites) withdrawn as a pure double-wrap of `Subflow.Descend`; `Execution.Dispatch` withdrawn (`ExecutionApi:Transport` is `http` in committed config). ~40 per-span listener tests replaced by **six catalogue-independent guards** (the per-span shape's demonstrated catch rate against this repo's real trace defects is 0/4). Four defects fixed as repairs of standing precedents, incl. three `Instance.Activation` spans per recovered timeout. | **C-02 Chair**: this work has no named consumer (three roles confirmed independently — dead Grafana dashboard, zero alerts) and sits behind an `APPROVED`-but-unbuilt P0 (E31, 51 stranded instances/30 d) and the unbuilt P1 observability floor — proceed now or after? **C-03 CLOSED by requester 2026-09-12: one PR**, reaffirmed after seeing the council's 5-1 recommendation; the five colliding files' items (state/data envelopes, BuildGate, hot-path tags) move to a follow-on PR rebased after `effective-status-fingerprint`, and the review-size risk is the requester's. Requester also accepted the catalogue cut and chose to await the Chair before any code. **C-04**: `CHAIR.md:66` says "all three episode fields"; there are **four** since `092b9415` — precedent text must be amended, same in `.claude/rules/vnext-workflow-developer.md:314,341`. Separate issues: double EF registration under `Verbose` (possible 2× `Db.*` in the config operators set during incidents), `BrokenCircuitException` escaping `Error.Transient`, double `BuildCallerHash` per 304. |
| 2026-09-17 | [2026-09-17-humantask-function-redesign](../../../ai-docs/agent-council/sessions/2026-09-17-humantask-function-redesign/DECISION.md) | Redesign the `human-task` domain function: root-projection predicate (`Type='R'` + `EffectiveStateSubType=Human` + `EffectiveStatus IN ('A','B')`), a 60 s cache with a client `CacheOverride` header, a replacement index, and leaf descent for authorization + payload | High | Split, **Awaiting Chair**: `APPROVED` (LIMIT+cap+truncation marker, drop counters, filtered correlations include) · `APPROVE_WITH_CONDITIONS` (phantom-task reset, leaf domain/version fix) · `EXPERIMENT_REQUIRED` (own-state predicate + index + literal constants) · `CLARIFICATION_REQUIRED` (the cache) · `REJECTED` (`EffectiveStatus` as a term, client `CacheOverride`, historical-version enumeration, root→leaf pointer, per-instance descent, `AsSplitQuery`, production mode fork, CHECK constraint) · `ESCALATE` (no authentication on the endpoint) | **Two of the three premises refuted from committed tests, 7/7.** `served effectiveStatus ∈ {A,B} ⟺ Status ∈ {A,B}` exactly (`IsTerminal ≡ {C,F,P}`), so substituting it lists cancelled/faulted instances as open human tasks (`InstanceEffectiveStatusTests.cs:127`, 15 measured rows) and drops live completion-window roots (`:161`); Security withdrew its own "AND it" hard constraint as strictly narrower. `Type='R'` is a **superset**, asymmetrically (`InstanceTypeTests.cs:114,134,172,190`). The third premise — leaf state is projected onto the root — is true as a mechanism and unusable as a membership basis: **`EffectiveStateSubType` has no reset writer anywhere**, so a parked parent carries a finished child's `subType=6` permanently and, because `Workflow.cs:545-547` appends well-known transitions from every state and an empty grant set allows, **the phantom row is listed for every caller on master today**; `P` children never notify any ancestor (`ChangeState` arms only `if (IsSubFlow)`); and the projection is event-delivered with two publish exclusions. **Selected instead: select by the instance's OWN state** — `Status IN ('A','B') AND CurrentStateSubType = 6 AND CurrentStateType IS DISTINCT FROM 4`, no `Type` term, no jsonb probe, no projection column. `CurrentStateSubType` is written unconditionally by `ChangeState` in the same UoW as the fact. This makes all three holes unreachable rather than fixed, and **dissolves the descent question the requester asked**: the waiting instance is found by its own domain's scan (child schemas are already opened and scanned every request — verified from `PublishAsync`→`MigrateSchemaAsync` with no type predicate) and morph-idm already unions every domain. **Remote calls added: zero.** Cache placement settled on evidence as **in front of the role filter** (dynamic grants read author-chosen headers via `AuthorizationRequestContext`, so behind-filter keys are provably non-enumerable — the same reason the rule-gated long-poll body is never cached; and per-caller hit rate is `1 − 1/f` ≈ 0). | **C-00 name the Chair** (`CHAIR.md` unconfigured). **C-01** Q1/Q2 never answered — no target, no baseline, no defect report; nothing was ranked on latency. **C-02** the endpoint has **no authentication** (zero repo-wide `[Authorize]`/`UseAuthentication`/`AddAuthentication(`; unsigned header identity; APISIX documented for `request-id`/`opentelemetry` only; no NetworkPolicy; no rate limiter) — Blocking on the cache, the default flip and any client `CacheOverride`, explicitly **not** blocking the rest; required wording carried verbatim. **C-04 re-ask the Chair on the 60 s**: the cited precedent gives child-owned state **500 ms with per-hit validation**, and that validation is a single-row projection that does not transplant to a list at any TTL. **E0 (`EXPLAIN`) gates the whole experiment tier** — nobody has ever seen the SQL this endpoint emits, and if it returns a seq scan then per-request cost is `Σ n_total × domains` (the only hypothesis all session predicting monotonic degradation with no deploy) and `IX_Instances_HumanTask` is a **net-negative index today** (per-tuple jsonb cast on every write, no read benefit). **R-1 is a required constituent, not a follow-up:** own-state drops every parent transition role override (`GetParentTransitionOverrides` returns null when `instance.Subflow == null`; the child-side reader's only caller is the state function) — a privilege **widening**, raised Blocking by the Devil's Advocate, conceded by the proposal's author and by Security and Pragmatic who had both asserted the opposite, downgraded to a one-line reader swap only after its own author verified the fix. **R-2:** children inherit `Key` from the parent and `Workflow` comes from `schema.Key`, so sibling human children collide on both — additive `Guid? Id` ships in the release before the flip. Five pre-existing defects filed separately (FlowVersion authorization, unscanned non-Active schemas, `state_role_overrides` dead write, a misleading comment that misled a proposal, the reset's non-`human-task` readers). **Nothing in this session was measured** — all four observability servers unreachable throughout. |
| 2026-09-21 | [2026-09-21-state-timeout-surfacing](../../../ai-docs/agent-council/sessions/2026-09-21-state-timeout-surfacing/DECISION.md) | Surface the workflow-level timeout on the state function as a countdown (client-sdk-core#59): separate `timeout` block, terminal guard, master-only scope, and two new `InstanceJob` columns for `key`/`target` | High | **`APPROVED` by Chair 2026-09-21** (reported by the requester) — was: split `APPROVED` (the `timeout` block · `reset` known-issue row) · `APPROVE_WITH_CONDITIONS` (shared effective-timeout resolver) · `CLARIFICATION_REQUIRED` (the fire-path fix) · `REJECTED` (the two `InstanceJob` columns + migration) | **The council found a runtime defect instead of a feature gap.** FACT-A, found independently by five of seven roles and verified by the facilitator: the **subflow timeout override is write-only for the armed instant** — `FlowTimeoutJobHandler.cs:96` bails on `workflow.Timeout is null`, `ApplyTimeoutStateStep.cs:28,37` takes the target from `context.Workflow.Timeout`, and the only readers of `MetaDataKeys.TimeoutOverride` in `src/` are the writer (`SubflowStarter.cs:152`) and the arm (`InstanceCommandAppService.cs:477`). A child armed by an override with no own `timeout` therefore advertises a deadline after which **nothing happens**. It reproduces on master against a shipped fixture: `subflow-orchestration-parent.json` declares `overrides.timeout = {key: child-push-timeout, target: child-cancelled, reset: OnEntry, PT15M}` while `subflow-orchestration-child.json` has `"timeout": null` and *does* declare `child-cancelled`; no test references it. **D4 (the requester's own decision) was withdrawn in round 2 by PROP-04, its author and only defender** — not on the vote (PROP-05 and PROP-06 accepted it) but because its justification collapsed: the fire path needs no column (`FlowTimeoutJobHandler.cs:68-70` loads the aggregate *above* the bail at `:93-99`; `ApplyTimeoutStateStep` already holds `context.Instance`), and the narrower read-path claim (avoid a per-poll deserialize) does not carry a per-schema production migration — the parse is `ContainsKey`-gated, off the 304 path, memoizable, and paid only by override-carrying children. Selected instead: **one shared effective-timeout resolver `override(ExtraProperties) ?? workflow.Timeout`, called by BOTH the read and the fire path**, so the two cannot diverge — the property D4 was reaching for by a more expensive route. Contract: a sibling `timeout` block (`{key, target, executeAtUtc}`), NOT a `transitions[]` entry (it cannot honour the v7 uniformity concession — no callable key — and `TransitionItem` has no `target`); emitted only while `instance.Status.IsTerminal` is false; polled instance only. `ResponseShapeVersion` v9→v10. **O1 (legacy-row fallback a/b/c) dissolved** — no columns, no legacy rows. PROP-01's proposed `scope:"workflow"` field **withdrawn by its author** (a leaf's timeout would carry it too, so it cannot discriminate the owner case D3 defers); PROP-06's past-due suppression **withdrawn by its author** ("two bodies under one ETag"). Cost settled from code, not benchmark: `GetListActiveAsync` has **no `JobType` predicate**, so the timeout row is already materialized on every full build — the change is a filter, not a query; PROP-03 and PROP-05 independently agreed call-count invariants discharge the no-unmeasured-claim rule *because the claim is structural, not temporal* (PROP-01 struck its p95 accordingly). Quotable bound: **a shape bump costs at most one full state build per (instance × callerHash) per generation per TTL, not one per poll** (`InstanceQueryAppService.cs:1507-1533`) — it does not bound Redis ops, egress, peak concurrency or cold JIT. **Two platform findings outliving this change:** (1) the facilitator's FACT-D (migrator runs after the app pods) was **wrong and retracted** — the orchestrator Deployment is itself a weight-4 `post-upgrade` hook vs the migrator's weight 3, so the migrator runs first; four objections built on it were withdrawn; (2) PROP-06 then established that **this chart has no mixed-version window at all** — every workload is a `post-upgrade` hook with `before-hook-creation` and no `strategy:` block, i.e. delete-then-create, an ordered cutover with an availability gap. A chart that already takes downtime per release cannot be harmed by an ETag flip. Chart fact, not production fact (ArgoCD would differ). | **All conditions closed 2026-09-21.** **C2 SATISFIED** — the requester withdrew D4 and chose the shared resolver. **C4 SATISFIED** — the requester accepted the production behaviour change outright ("canlıda akışlar timeout'a düşebilir sorun değil, biz bunu dökümante ederiz"), selected *fix* over *remove*, and directed **one PR** instead of the council's two-change sequencing; both are recorded as requester deviations. **C1** is recorded on the requester's report of Chair approval, not on an observed Chair message. Original gate text follows. **C2 was the gate that mattered: the requester had to confirm D4's withdrawal** — the council overturned a standing requester decision on evidence the requester did not have; if reaffirmed, the design inverts to the columns. **C4**: the fire-path fix is a *production behaviour change* (children that never cancelled start cancelling) — scope it as its own change landing first in the same train (write-path only, no shape bump), and decide fix-vs-remove for an override the engine has never honoured. **C5**: PROP-03's G4 and PROP-07's consensus attack independently found the same trap — after the fix, `subflow-orchestration-child`'s block goes from *correctly absent* to *incorrectly absent*, no shape change, no detector; G4 is mandatory and the read's source must be coupled to the fix's final rule. **C3**: #59 must record an explicit "will read / will not read" — the issue's "client cost: zero" is stale (`dataSourceState.ts` no longer exists; `executeAtUtc`/`scheduledAt` return zero hits on client master `963cbe42`) and `kind:"scheduled"` has had zero consumers since it shipped. **C6**: `TimerConfig.reset` is read nowhere yet is a required field, and 2 of 2 corpus declarations say `OnEntry` — `known-issues.json` row + one contract sentence, same commit. **C8**: the resolver extraction is a precondition; if declined, hold the read (O1 then reverts to `(b′)`). **Nothing was measured** — all four observability servers unreachable throughout; integration test in vnext-example is mandatory per the Chair precedent. Self-corrections on the record: PROP-03 ×3 (all one error — verifying one half of a comparison), PROP-06 ×2, PROP-05 ×2, PROP-07 ×2, PROP-04 wholesale, facilitator ×1. **IMPLEMENTED AND VERIFIED ON A LOCAL RUNTIME 2026-09-21** (uncommitted). The `timeout-lab` integration scenario earned itself immediately: it failed on the first run and exposed a **fourth** reader of `workflow.Timeout` that the council's code reading had missed — `CreateTransitionRecordStep` resolves `$timeout` at **order 20** via `Workflow.ResolveWellKnownKey`, which THROWS `TimeoutNotConfiguredForWorkflowException` for an override-driven child, killing the pipeline three steps before `ApplyTimeoutStateStep` (38) could apply the fix. Because `SetBusy` (19) had already committed, the measured symptom was not 'the timeout does not fire' but the child left **Busy in its waiting state forever** with its job row marked processed (instance `e48ac9e2…`, still in the bench DB as the before-picture). Fixed at that call site; `ResolveWellKnownKey` left alone. Final evidence, from postgres and the host logs rather than the test summary: children reach `child-timedout`/`C` with the audit key **`child-abandoned`** — the PARENT's override key — fired ~54 ms after the armed `ExecuteAt`; root flow reaches `root-timedout`/`C` via `root-abandoned`; zero `TimeoutConfigMissing` in the log. Tests: `timeout-lab` 3/3, neighbouring `SubflowOrchestration` + `ChainBusy` + `ScheduleAfterAuto` 39/39, unit suites at the measured master baseline (Domain 28/2500, Application 21/2197 — identical failure NAMES, verified by stash). |
| 2026-09-22 | [2026-09-22-remove-execution-authorization](../../../ai-docs/agent-council/sessions/2026-09-22-remove-execution-authorization/DECISION.md) | Remove role-based authorization from every execution/read surface (nine `queryRoles` 403 gates + the long-poll ack), leaving roles for visibility, with the Internal Gateway's `check-authorize` calling vNext's `authorize` as the single decision point | High | **v2 after requester decision**, Awaiting Chair: `APPROVED` (`authorize` totality fix + `ack` target — filed as a defect fix) · `APPROVE_WITH_CONDITIONS` (Group A **and**, after the requester closed C-02, Group B) · `APPROVE_WITH_CONDITIONS` (ack, HC-1 as a sequencing gate) | **The premise is not true in production.** `check-authorize` is real and live (it guards EFT/wire/FX) but covers **25/1394** intprod routes, **0/1793** dmzprod and **0 of the 60 vNext `/workflows` routes**; `…/functions/state` carries only proxy-rewrite+cors and `POST …/longpoll/ack` only cors+proxy-rewrite. The facilitator closed the one refuting path: **no `check-authorize`, no `forward-auth`, no auth plugin in ANY global rule on either production gateway**. So for these routes the change deletes a control rather than moving one. **The requester's own F5 premise was tested and holds for 5 of 9 surfaces, not 9** — `tasks`, `actions`, `incidents/active`, `incidents` have **no ungated sibling** (`GetInstanceTasksAsync` has no reader outside `IInstanceQueryAppService`; `IInstanceIncidentRepository`'s only other production caller is a write path; the state body carries incidents as **links, not content**), which is why the reads split. Two roles reached the two halves from opposite directions and, asked to reconcile, confirmed complementarity — *one measured F5's robustness, the other its coverage* — and a third showed the same line explains the test surface: **the surfaces whose removal no test could detect are exactly the ones with ungated siblings.** Group B's live exposure is narrower than four, recorded against the council's own widened position: `actions` gates an **empty list** (`InstanceActions` has no writer, ever) and `tasks` is metadata-only — **the incident pair is the real content**, and the only green refusal evidence anywhere (`SecuredIncidentsTests` 31/31, 2026-09-07). **Undeterminable and therefore a hard constraint:** which `CallerRoleProvider` production runs — under `default` roles come from an unsigned caller-set header (never a control), under `morph-idm` the resolver **never reads a role header at all** and a `204` yields an empty set (a real control). Shipped default is `default`, **zero `CallerRoleProvider` hits in all of `vnext-helm-charts`**, but `extraEnvConfig` permits an out-of-chart override — and an unrecognised value **silently degrades to the header-trusting default with no log**, hence **HC-3: establish the provider by runtime observation of a pod, never from config or an operator's statement**. Also found: `authorize` is **strictly weaker than the gate it replaces** — the state function gates at root AND leaf (two conjunctive gates) while `authorize?queryRoles=true` short-circuits into the subflow (`AuthorizeAppService.cs:62,95-142`) and never evaluates the root's own `queryRoles`; a live defect on master. Selected: staged flip (P1 no-op in-repo → P2 attach 60 gateway routes, **named owner required** → P3 per-environment flip as its own canary, 90 days, fallback = Group B keeps its gates permanently). Enforcement flag **kept and defaulted to `true`** — both no-flag arguments were conceded, because `helm rollback` is a **no-op for every vNext workload** (zero pre/post-rollback hooks) so recovery is a release cycle. Totality invariant rebounded from unbounded to *403-producing gates only*. **Net-costlier, structurally: 304 path 1→4 SQL and 1→2 inbound, build path 3→6 SQL, worse by `3(d+1)` at subflow depth `d`** — no speed claim may be made. **Effort 4.5 → ~11.5 d under objection, and risk is inversely distributed: 1.8 of 8 runtime days carry all the security risk, 2.0 carry none and fix the defect.** Five of seven roles changed a position on the record; facilitator self-corrected once (F4 named a third gate caller that does not exist — the correction exposed that the long-poll **arm** decision reads only the `roles` arm, so a `rule`-arm state arms the pause for every caller). | **C-01 the Devil's Advocate's "this is tidiness" was left standing by every role** — no incident, no defect report, no measurement motivates the work, against an unbuilt P0 backlog; **C-02 excluding Group B narrows the requester's ask** — if reaffirmed, the verdict inverts to `BLOCKED` on HC-3 evidence rather than exclusion; **C-03 precedent amendment**: `CHAIR.md`'s "anything that must be an actual boundary belongs in `queryRoles` (which **is** enforced, with 403)" becomes false for Group A on merge; **C-04** name the APISIX owner for the 60-route attachment; **C-05 R-2 is the larger finding and not this session's to fix** — the runtime has **no authentication at all** (zero `[Authorize]`, zero `UseAuthentication`, zero rate limiter, unsigned header identity, no NetworkPolicy in the chart's own templates), escalated by two prior sessions and still open; this session was the dedicated security session both asked for and confirms it without resolving it. Filed separately: `AuthorizationMatrixFunctionHandler` serves the full grant matrix **ungated** (seventh surface); the `internal/*` endpoints' documented "network isolation" is only `[ApiExplorerSettings(IgnoreApi=true)]`, a Swagger attribute, and `internal/related-data/batch` returns 100 unfiltered instance snapshots to anyone reaching the pod. **Nothing was measured** — all four observability servers unreachable throughout, and `role-matrix-lab` has never been run, so no green baseline exists. **v2 addendum 2026-09-22 — requester closed C-02 and reaffirmed Group B**, supplying the fact the council lacked: **the `check-authorize` rules are downstream of this work, not missing from it.** DA-1 is therefore re-characterised, not refuted — the measurement stands, its meaning changes from "a control is deleted with no successor" to "the successor is sequenced after the producer". Group B inverts to `APPROVE_WITH_CONDITIONS` exactly as §C-02 committed in advance. **The enforcement switch becomes load-bearing and is what makes that sequencing enforceable rather than intentional**: defaulting to `true`, the merge changes no observable behaviour anywhere, the gates become switchable rather than deleted, and HC-2′/HC-3 become **per-environment flip gates instead of merge gates** — which is what the requester's ordering asks for. Nothing is deleted at merge; deletion of the switch and the dead code is a later release after the last environment flips, per the repo's parallel-support policy. Requester also made **vnext-example integration tests mandatory** (merge gate, not follow-up) — which surfaces its own deliverable: `RoleMatrixLab` has never been executed, so **a green first run is itself part of the work**. Plan: `IMPLEMENTATION-PLAN.md` — groups A (authorize totality + `ack`, 2.0 d, no gate touched, mergeable alone and fixes the live defect) → B (switch, 1.5 d) → C (instrumentation, 0.5 d) → D (docs/meta, 0.75 d) → integration 2.0 d → unit 1.0 d; runtime 7.75 d, all-in ~10.75 d excluding the gateway's 60-route work. Residual risk the requester accepted once, stated and not repeated: between flip and rule-attachment, `incidents/active` + `incidents` have no control anywhere. **v3, 2026-09-23 — requester ruling: no switch, full removal.** Offered enforce-by-default / delegate-by-default / delete, the requester chose delete: `IsInstanceQueryAllowedAsync`, the ack gate, `Workflow:Authorization:EnforceInProcess` and its instrumentation (counter + EventIds 50046/50047/50048) are gone from the code. The council's reasoning is unchanged and its conditions survive the ruling — HC-2′ and C-04 simply moved from post-merge to **pre-production**, because the residual risk above is no longer bounded by a flip an environment controls. Implementation found four further defects the council did not predict, three in `authorize` itself (the gate read `EffectiveState`, so a parent's narrowing stopped applying once its child had a subflow of its own; state transitions were state-gated through `availableIn`, which they do not carry, making the oracle permissive; the `interaction` block was emitted from the state's declaration rather than from a pending ack) and one found by probing after the header rule was already green — the `role` QUERY parameter overrode an authority provider's empty answer, the same hole on a different channel. |
<!-- newest at the bottom; keep one line per session -->

Verdicts: `APPROVED` · `APPROVE_WITH_CONDITIONS` · `EXPERIMENT_REQUIRED` · `CLARIFICATION_REQUIRED` · `BLOCKED` · `REJECTED`.
`Follow-up` links the implementation PR/issue, the superseding session, or `—`.
