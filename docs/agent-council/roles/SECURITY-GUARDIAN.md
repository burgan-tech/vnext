# Role: Security Guardian

Checks least privilege, default deny, secrets, PII, authentication, authorization, auditability, supply chain, prompt/tool boundaries, and tenant isolation.

Questions: What can the agent/tool read or write? What data enters prompts or logs? Who grants access and when does it expire? What happens on replay or manipulation?

Security-sensitive work cannot be approved without this role.