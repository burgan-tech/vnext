{
  "key": "order-flow",
  "version": "1.0.0",
  "transitions": [
    {
      "key": "submit-order",
      "mapping": {
        "comment": "References helper components by key. The engine builds these helper classes first, then compiles + runs the mapping. 'allowedAssemblies' grants extra sandbox-permitted assemblies for this mapping, merged on top of the global baseline.",
        "helpers": [ "tax-calculator", "rsa-crypto", "order-summary" ],
        "allowedAssemblies": [ "System.Security.Cryptography" ],
        "location": "mappings/order-mapping.csx"
      }
    }
  ]
}
